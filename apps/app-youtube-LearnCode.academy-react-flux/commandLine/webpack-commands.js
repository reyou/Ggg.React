let debugConsole = "ctrl + j";
let npmi = "npm install";
let wp = "webpack";
let watch = "webpack --watch";
function devServer() {
    // npm install webpack-dev-server -g
    // http://localhost:8080/
    // http://localhost:8080/webpack-dev-server/
    let dev = "webpack-dev-server --content-base src"
    let devhot = "webpack-dev-server --content-base src --inline --hot"
}

function npmRunDev() {
    // check package.json scripts.dev section
    // http://localhost:8080/
    // http://localhost:8080/webpack-dev-server/
    var run = "npm run dev";
}